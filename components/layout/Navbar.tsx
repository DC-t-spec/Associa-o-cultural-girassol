'use client'; import Link from 'next/link'; import { useEffect,useState } from 'react'; import { Button } from '@/components/ui/Button'; import { GirassolLogo } from '@/components/ui/GirassolLogo'; import { AdminLogoLink } from '@/components/ui/AdminLogoLink'; import { MobileMenu } from './MobileMenu';
const links=[['#inicio','Início'],['#quem-somos','Quem Somos'],['#historia','História'],['#projectos','Projectos'],['#impacto','Impacto'],['#galeria','Galeria'],['#noticias','Notícias'],['#parceiros','Parceiros'],['#contacto','Contacto']].map(([href,label])=>({href,label}));
export function Navbar(){const[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>30);f();addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);return <header className={`fixed inset-x-0 top-0 z-40 transition ${scrolled?'bg-black/85 shadow-2xl backdrop-blur':'bg-transparent'}`}><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3"><AdminLogoLink href="/#inicio" className="flex items-center gap-3"><GirassolLogo compact className="p-1"/><span className="hidden font-display text-xl font-bold text-white sm:block">Girassol</span></AdminLogoLink><nav className="hidden items-center gap-5 text-sm text-zinc-200 md:flex">{links.map(l=><Link className="hover:text-sun" key={l.href} href={l.href}>{l.label}</Link>)}<Button href="/fiti" className="px-5 py-2">FITI</Button></nav><MobileMenu links={[...links,{href:'/fiti',label:'FITI'}]}/></div></header>}
