'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { useEffect,useState } from 'react'; import { Button } from '@/components/ui/Button'; import { GirassolLogo } from '@/components/ui/GirassolLogo'; import { ManagedLogo } from '@/components/ui/ManagedLogo'; import { MobileMenu } from './MobileMenu'; import type { ThemeSettings } from '@/types/cms';
const links=[{href:'/#quem-somos',label:'Quem Somos'},{href:'/#projectos',label:'Projectos'},{href:'/#noticias',label:'Notícias'},{href:'/#contacto',label:'Contacto'}];
export function Navbar({settings}:{settings?:ThemeSettings}){const router=useRouter();const[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>30);f();addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);return <header className={`fixed inset-x-0 top-0 z-40 transition ${scrolled?'bg-black/85 shadow-2xl backdrop-blur':'bg-transparent'}`}><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3"><Link href="/#inicio" onDoubleClick={(event)=>{event.preventDefault();router.push('/admin');}} className="flex items-center gap-3"><ManagedLogo debugLabel="Navbar" settingKey="site_logo_url" alt="Associação Cultural Girassol" className="h-12 w-auto object-contain" fallback={<GirassolLogo/>}/><span className="hidden font-display text-xl font-bold text-white sm:block">Girassol</span></Link><nav className="hidden items-center gap-5 text-sm text-zinc-200 md:flex">{links.map(l=><Link className="hover:text-sun" key={l.href} href={l.href}>{l.label}</Link>)}<Button href="/fiti" className="px-5 py-2">FITI</Button></nav><MobileMenu links={[...links,{href:'/fiti',label:'FITI'}]}/></div></header>}
