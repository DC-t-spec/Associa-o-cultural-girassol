import { news } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Button } from '@/components/ui/Button';
export function News(){return <section id="noticias" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Notícias" title="Actualizações"/><div className="grid gap-5 md:grid-cols-2">{news.map(n=><Card key={n.title}><div className="mb-4 aspect-video rounded-2xl bg-zinc-800"/><p className="text-sm uppercase tracking-widest text-sun">{n.category} • {n.date}</p><h3 className="mt-2 text-2xl font-bold text-white">{n.title}</h3><p className="mt-2 text-zinc-400">{n.summary}</p><Button href="#contacto" variant="ghost" className="mt-5">Ler mais</Button></Card>)}</div></div></section>}
