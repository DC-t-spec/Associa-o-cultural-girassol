import { timeline } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Sparkles } from 'lucide-react';
export function Timeline(){return <section id="historia" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="História" title="Linha do tempo"/><div className="grid gap-5 md:grid-cols-3">{timeline.map(i=><Card key={i.year}><Sparkles className="mb-4 text-sun"/><p className="text-3xl font-black text-ember">{i.year}</p><h3 className="mt-3 text-xl font-bold text-white">{i.title}</h3><p className="mt-2 text-zinc-400">{i.description}</p></Card>)}</div></div></section>}
