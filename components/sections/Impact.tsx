import { impactStats } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { Counter } from '@/components/ui/Counter'; import { SectionTitle } from '@/components/ui/SectionTitle';
export function Impact(){return <section id="impacto" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Impacto" title="Números editáveis"/><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{impactStats.map(s=><Card key={s.label}><p className="font-display text-5xl font-black text-sun"><Counter value={s.value} suffix={s.suffix}/></p><p className="mt-3 text-zinc-300">{s.label}</p></Card>)}</div></div></section>}
