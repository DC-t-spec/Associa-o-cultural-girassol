import { impactStats } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle';
export function Impact(){return <section id="impacto" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Impacto" title="Indicadores institucionais"/><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{impactStats.map(s=><Card key={s}><p className="text-lg font-bold text-sun">{s}</p></Card>)}</div></div></section>}
