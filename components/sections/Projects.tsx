import { projects } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { Button } from '@/components/ui/Button'; import { SectionTitle } from '@/components/ui/SectionTitle';
export function Projects(){return <section id="projectos" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Projectos" title="Programas estruturantes"/><div className="grid gap-6 md:grid-cols-2">{projects.map(p=><Card key={p.title} className={p.link?'md:col-span-2 bg-gradient-to-br from-ember/20 to-sun/10':''}><h3 className="text-2xl font-bold text-white">{p.title}</h3><p className="mt-3 text-zinc-300">{p.description}</p>{p.link&&<Button href={p.link} className="mt-6">Ver página do FITI</Button>}</Card>)}</div></div></section>}
