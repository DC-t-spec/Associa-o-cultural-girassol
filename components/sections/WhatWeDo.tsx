'use client';
import { whatWeDo } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Theater } from 'lucide-react'; import { useSection, text } from '@/components/publicCms';
export function WhatWeDo(){const f=useSection('home_what_we_do');let items:any[]=whatWeDo;try{if(f.cards) items=JSON.parse(String(f.cards));}catch{}return <section id="o-que-fazemos" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow={text(f.eyebrow,'O que fazemos')} title={text(f.section_title,'Arte com impacto social')}/><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map(t=><Card key={t.title}><Theater className="mb-4 text-sun"/><h3 className="text-xl font-bold text-white">{t.title}</h3><p className="mt-2 text-zinc-400">{t.description}</p></Card>)}</div></div></section>}
