import { whatWeDo } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Theater } from 'lucide-react';
export function WhatWeDo(){return <section className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="O que fazemos" title="Arte com impacto social"/><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{whatWeDo.map(t=><Card key={t}><Theater className="mb-4 text-sun"/><h3 className="text-xl font-bold text-white">{t}</h3><p className="mt-2 text-zinc-400">Área editável para descrever iniciativas, actividades e impacto.</p></Card>)}</div></div></section>}
