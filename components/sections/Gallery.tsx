'use client'; import { useState } from 'react'; import { gallery } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle';
export function Gallery(){const [filter,setFilter]=useState('Todos'); const cats=['Todos',...gallery.map(g=>g.category)]; const items=filter==='Todos'?gallery:gallery.filter(g=>g.category===filter); return <section id="galeria" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Galeria" title="Memória visual"/><div className="mb-8 flex flex-wrap justify-center gap-3">{cats.map(c=><button className={`rounded-full px-4 py-2 ${filter===c?'bg-sun text-black':'bg-white/10 text-white'}`} onClick={()=>setFilter(c)} key={c}>{c}</button>)}</div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map(g=><Card key={g.category}><div className="mb-4 aspect-video rounded-2xl bg-gradient-to-br from-sun/25 to-ember/25"/><p className="text-sun">{g.category}</p><h3 className="text-xl font-bold text-white">{g.title}</h3><p className="text-zinc-400">{g.description}</p></Card>)}</div></div></section>}
