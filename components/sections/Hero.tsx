import { Button } from '@/components/ui/Button'; import { AdminLogoLink } from '@/components/ui/AdminLogoLink'; import { GirassolLogo } from '@/components/ui/GirassolLogo'; import { ManagedLogo } from '@/components/ui/ManagedLogo'; import { VideoBackground } from '@/components/ui/VideoBackground'; import { hero } from '@/lib/data'; import { getThemeSettings } from '@/lib/cms';
export async function Hero(){const settings=await getThemeSettings();const logoUrl=settings.hero_logo_url||settings.site_logo_url;return <section id="inicio" className="relative flex min-h-screen items-center overflow-hidden pt-24"><VideoBackground src="/videos/girassol-hero.mp4"/><div className="absolute inset-0 bg-gradient-to-b from-black/60 via-black/70 to-black"/><div className="relative mx-auto max-w-7xl px-4"><AdminLogoLink href="/#inicio" className="mb-8 inline-flex"><ManagedLogo src={logoUrl} alt={settings.site_logo_alt} imageClassName="max-h-32"><GirassolLogo/></ManagedLogo></AdminLogoLink><p className="mb-3 text-sm font-bold uppercase tracking-[.4em] text-sun">Teatro • juventude • memória</p><h1 className="max-w-4xl font-display text-6xl font-black text-white md:text-8xl">{hero.title}</h1><p className="mt-6 max-w-2xl text-xl text-zinc-100">{hero.subtitle}</p><p className="mt-5 max-w-3xl text-lg leading-8 text-zinc-300">{hero.support}</p><div className="mt-9 flex flex-wrap gap-4"><Button href="#quem-somos">{hero.primary}</Button><Button href="/fiti" variant="ghost">{hero.secondary}</Button></div></div></section>}
