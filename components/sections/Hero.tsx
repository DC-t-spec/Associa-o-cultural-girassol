'use client';
import { Button } from '@/components/ui/Button'; import { AdminLogoLink } from '@/components/ui/AdminLogoLink'; import { GirassolLogo } from '@/components/ui/GirassolLogo'; import { ManagedLogo } from '@/components/ui/ManagedLogo'; import { VideoBackground } from '@/components/ui/VideoBackground'; import { useSection, text } from '@/components/publicCms';
export function Hero(){const f=useSection('home_hero');return <section id="inicio" className="relative flex min-h-screen items-center overflow-hidden pt-24"><VideoBackground src={text(f.background_video_url,'/videos/girassol-hero.mp4')}/><div className="absolute inset-0 bg-gradient-to-b from-black/60 via-black/70 to-black"/><div className="relative mx-auto max-w-7xl px-4"><AdminLogoLink href="/#inicio" className="mb-8 inline-flex"><ManagedLogo debugLabel="Hero" settingKey="hero_logo_url" fallbackKeys={["site_logo_url"]} alt="Associação Cultural Girassol" className="h-16 w-auto object-contain" fallback={<GirassolLogo/>}/></AdminLogoLink><p className="mb-3 text-sm font-bold uppercase tracking-[.4em] text-sun">{text(f.eyebrow,'Teatro • juventude • memória')}</p><h1 className="max-w-4xl font-display text-6xl font-black text-white md:text-8xl">{text(f.title,'Associação Cultural Girassol')}</h1><p className="mt-6 max-w-2xl text-xl text-zinc-100">{text(f.subtitle,'Teatro, juventude, memória e transformação cultural em Moçambique.')}</p><p className="mt-5 max-w-3xl text-lg leading-8 text-zinc-300">{text(f.description)}</p><div className="mt-9 flex flex-wrap gap-4"><Button href={text(f.primary_button_link,'#quem-somos')}>{text(f.primary_button_text,'Conheça a Associação')}</Button><Button href={text(f.secondary_button_link,'/fiti')} variant="ghost">{text(f.secondary_button_text,'Ver FITI')}</Button></div></div></section>}
