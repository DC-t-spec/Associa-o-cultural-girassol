'use client';
import type { PageSection, SectionField } from '@/types/cms';import { EditableField } from './EditableField';
export function SectionEditor({section,fields}:{section:PageSection;fields:SectionField[]}){return <section id={section.section_name} className="scroll-mt-8 rounded-3xl border border-white/10 bg-zinc-950/80 p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-display text-2xl font-bold text-sun">{section.section_name}</h3><p className="text-sm text-zinc-400">Chave: {section.section_key} · ordem {section.order_index}</p></div><div className="flex gap-2 text-xs"><button className="rounded-full border border-white/10 px-3 py-2">Pré-visualizar</button><button className="rounded-full border border-white/10 px-3 py-2">Mover ↑</button><button className="rounded-full border border-white/10 px-3 py-2">Mover ↓</button><button className="rounded-full bg-sun px-3 py-2 font-bold text-black">Guardar</button></div></div><div className="mt-5 grid gap-4 md:grid-cols-2">{fields.map(f=><EditableField key={f.id} field={f}/>)}</div></section>}
