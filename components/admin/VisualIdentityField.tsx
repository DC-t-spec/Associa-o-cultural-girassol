'use client';

import { useEffect, useState } from 'react';

type Props = { settingKey: string; label: string; description?: string; defaultValue: string; type?: 'url'|'text'|'number'|'boolean'; onSave: (key:string,value:string)=>Promise<void> };
export function VisualIdentityField({settingKey,label,description,defaultValue,type='url',onSave}:Props){const[value,setValue]=useState(defaultValue);const[saved,setSaved]=useState(false);useEffect(()=>setValue(defaultValue),[defaultValue]);async function save(){await onSave(settingKey,value);setSaved(true);setTimeout(()=>setSaved(false),1800)}return <div className="rounded-2xl border border-white/10 bg-black/30 p-4"><label className="block text-sm font-semibold text-zinc-100">{label}</label>{description&&<p className="mt-1 text-xs text-zinc-400">{description}</p>}{type==='boolean'?<select value={value} onChange={e=>setValue(e.target.value)} className="mt-3 w-full rounded-2xl border border-white/10 bg-black/40 px-4 py-3 text-white outline-none focus:border-sun"><option value="true">Activo</option><option value="false">Inactivo</option></select>:<input value={value} onChange={e=>setValue(e.target.value)} type={type==='number'?'number':type==='url'?'url':'text'} step={type==='number'?'0.01':undefined} placeholder={settingKey} className="mt-3 w-full rounded-2xl border border-white/10 bg-black/40 px-4 py-3 text-white outline-none focus:border-sun"/>}{type==='url'&&value.trim()&&<div className="mt-3 rounded-xl border border-white/10 bg-zinc-950 p-3"><p className="mb-2 text-xs text-zinc-500">Preview</p><img src={value} alt={label} className="max-h-24 w-auto object-contain" onError={e=>{e.currentTarget.style.display='none'}}/></div>}<button type="button" onClick={save} className="mt-3 rounded-full bg-sun px-4 py-2 text-sm font-bold text-black">{saved?'Guardado':'Guardar'}</button></div>}
