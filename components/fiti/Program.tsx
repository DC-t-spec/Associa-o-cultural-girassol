'use client'; import { useState } from 'react'; import { program } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Button } from '@/components/ui/Button';
export function Program(){const [cat,setCat]=useState('Todos'); const cats=['Todos','Teatro adulto','Teatro infantil','Teatro de rua','Teatro comunitário','Oficina','Masterclass','Debate','Teatro']; const items=cat==='Todos'?program:program.filter(p=>p.category===cat); return <section id="programacao" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Programação" title="Agenda filtrável"/><div className="mb-8 flex flex-wrap justify-center gap-3">{cats.map(c=><button className={`rounded-full px-4 py-2 ${cat===c?'bg-ember text-black':'bg-white/10 text-white'}`} onClick={()=>setCat(c)} key={c}>{c}</button>)}</div><div className="grid gap-5 md:grid-cols-2">{items.map(p=><Card key={p.title}><p className="text-sun">{p.date} • {p.time} • {p.venue}</p><h3 className="mt-2 text-2xl font-bold text-white">{p.title}</h3><p className="text-zinc-300">{p.company} {p.country}</p><p className="mt-3 text-zinc-400">{p.synopsis}</p><div className="mt-5 flex gap-3"><Button href="#bilhetes">Reservar</Button><Button href="#programacao" variant="ghost">Ver detalhes</Button></div></Card>)}</div></div></section>}
