import { companies } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Button } from '@/components/ui/Button';
export function Companies(){return <section id="companhias" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Companhias" title="Artistas e grupos"/><div className="grid gap-5 md:grid-cols-3">{companies.map(c=><Card key={c.name}><div className="mb-4 aspect-square rounded-3xl bg-white/5"/><h3 className="text-xl font-bold text-white">{c.name}</h3><p className="text-sun">{c.country}</p><p className="mt-2 text-zinc-400">{c.description}</p><p className="mt-2 text-zinc-300">{c.show}</p><Button href="#programacao" variant="ghost" className="mt-5">Ver programação</Button></Card>)}</div></div></section>}
