'use client';
import { Card } from '@/components/ui/Card'; import { Button } from '@/components/ui/Button'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { useSection, text } from '@/components/publicCms';
const fallbackItems=['Informações oficiais','Materiais de divulgação','Contactos de imprensa'];
export function PressKit(){const f=useSection('fiti_press');let items:any[]=fallbackItems.map(title=>({title,description:'A organização disponibiliza conteúdos institucionais mediante pedido e confirmação dos dados de contacto.'}));try{if(f.cards) items=JSON.parse(String(f.cards));}catch{}return <section id="imprensa" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow={text(f.eyebrow,'Imprensa')} title={text(f.section_title,'Imprensa')}><span>{text(f.section_text,'Área dedicada a jornalistas, órgãos de comunicação, parceiros e profissionais interessados em informações oficiais, materiais de divulgação, imagens, comunicados e contactos de imprensa.')}</span></SectionTitle><div className="grid gap-5 md:grid-cols-3">{items.map(i=><Card key={i.title}><h3 className="text-xl font-bold text-white">{i.title}</h3><p className="mt-2 text-zinc-400">{i.description}</p><Button href={i.url || '#contacto'} variant="ghost" className="mt-5">{i.button || 'Solicitar informação'}</Button></Card>)}</div></div></section>}
