import { workshops } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Button } from '@/components/ui/Button';
export function Workshops(){return <section id="oficinas" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Oficinas" title="Formação e masterclasses"/><div className="grid gap-5 md:grid-cols-2">{workshops.map(w=><Card key={w.title}><h3 className="text-2xl font-bold text-white">{w.title}</h3><p className="text-sun">{w.trainer}</p><p className="mt-3 text-zinc-300">{w.date} • {w.time} • {w.venue}</p><p className="text-zinc-400">Vagas: {w.vacancies} • Público: {w.target}</p><Button href="#formularios" className="mt-5">Inscrição</Button></Card>)}</div></div></section>}
