'use client';
import { workshops } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Button } from '@/components/ui/Button'; import { useRows } from '@/components/publicCms';
export function Workshops(){const rows=useRows('fiti_workshops',workshops as any[],q=>q.eq('is_active',true).order('date'));return <section id="oficinas" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow="Oficinas" title="Masterclasses e formação"/><div className="grid gap-5 md:grid-cols-3">{rows.map((w:any)=><Card key={w.id||w.title}><h3 className="text-xl font-bold text-white">{w.title}</h3><p className="text-sun">{w.trainer}</p><p className="mt-2 text-zinc-400">{w.date} • {w.time} • {w.venue}</p><p className="mt-2 text-zinc-300">{w.target}</p><p className="mt-2 text-zinc-400">Vagas: {w.vacancies}</p><Button href="#formularios" variant="ghost" className="mt-5">Inscrever</Button></Card>)}</div></div></section>}
